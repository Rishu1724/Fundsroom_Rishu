services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: fundsroom
    ports:
      - "5432:5432"
    volumes:
      - fundsroom_postgres_data:/var/lib/postgresql/data

volumes:
  fundsroom_postgres_data: